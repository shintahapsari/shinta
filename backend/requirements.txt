# Slim, public-PyPI-compatible dependencies for deploying the backend
# outside Emergent (Railway / Render / Fly / any host).
# The full Emergent build list is preserved in requirements-emergent.txt
# (contains extra libraries that are NOT used by the app code).

fastapi>=0.110,<1.0
uvicorn[standard]>=0.25
starlette>=0.37,<1.0
motor>=3.3,<4.0
pymongo>=4.6,<5.0
pydantic[email]>=2.6,<3.0
email-validator>=2.1
PyJWT>=2.8
bcrypt>=4.1
passlib>=1.7
python-multipart>=0.0.9
python-dotenv>=1.0
openpyxl>=3.1
reportlab>=4.1
pillow>=10.0
httpx>=0.27
requests>=2.31
resend>=2.0.0
